name: Pardora
options:
  bundleIdPrefix: net.pardev
  deploymentTarget:
    iOS: "17.0"
    watchOS: "10.0"
settings:
  base:
    DEVELOPMENT_TEAM: QMLVG482FY
    CODE_SIGN_STYLE: Automatic
    SWIFT_VERSION: "6.0"
targets:
  Pardora:
    type: application
    platform: iOS
    sources:
      - Pardora
    dependencies:
      - target: PardoraLiveActivityExtension
        embed: true
      - sdk: ActivityKit.framework
      - sdk: CarPlay.framework
      - sdk: WatchConnectivity.framework
      - target: PardoraWatchApp
        embed: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: net.pardev.pardora
        CODE_SIGN_ENTITLEMENTS: Pardora/Pardora.entitlements
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Pardora/Resources/Info.plist
        MARKETING_VERSION: "0.1"
        CURRENT_PROJECT_VERSION: "1"
        SUPPORTS_LIVE_ACTIVITIES: YES
  PardoraLiveActivityExtension:
    type: app-extension
    platform: iOS
    sources:
      - path: PardoraLiveActivityExtension
      - path: Pardora/Support/PardoraActivityAttributes.swift
    dependencies:
      - sdk: WidgetKit.framework
      - sdk: ActivityKit.framework
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: net.pardev.pardora.liveactivity
        GENERATE_INFOPLIST_FILE: YES
        MARKETING_VERSION: "0.1"
        CURRENT_PROJECT_VERSION: "1"
        SUPPORTS_LIVE_ACTIVITIES: YES
        SWIFT_VERSION: "6.0"
    info:
      path: PardoraLiveActivityExtension/Info.plist
      properties:
        CFBundleDisplayName: Pardora Activity
        CFBundleShortVersionString: "0.1"
        CFBundleVersion: "1"
        NSSupportsLiveActivities: true
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
  PardoraWatchApp:
    type: application
    platform: watchOS
    sources:
      - PardoraWatchApp
    dependencies:
      - sdk: WatchConnectivity.framework
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: net.pardev.pardora.watchapp
        GENERATE_INFOPLIST_FILE: YES
        MARKETING_VERSION: "0.1"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "6.0"
    info:
      path: PardoraWatchApp/Info.plist
      properties:
        CFBundleDisplayName: Pardora
        WKApplication: true
        WKCompanionAppBundleIdentifier: net.pardev.pardora
  PardoraTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - PardoraTests
    dependencies:
      - target: Pardora
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
schemes:
  Pardora:
    build:
      targets:
        Pardora: all
    test:
      targets:
        - PardoraTests
